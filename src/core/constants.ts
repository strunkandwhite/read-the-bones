/** Minimum number of match results needed for confident win rate statistics. */
export const MIN_SAMPLE_SIZE = 5;

/** Default number of seats in a draft when not specified. */
export const DEFAULT_NUM_SEATS = 10;
